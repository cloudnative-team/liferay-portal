{{- if and .Values.licensing.enabled .Values.licensing.onlineActivationWorkflow.enabled -}}
{{- $onlineActivationWorkflow := .Values.licensing.onlineActivationWorkflow -}}
{{- $image := printf "%s:%s" $onlineActivationWorkflow.image.repository (toString $onlineActivationWorkflow.image.tag) -}}
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
    labels:
        {{- include "liferay.labels" . | nindent 8 }}
    name: online-activation-workflow-template
    namespace: {{ include "liferay.namespace" . }}
spec:
    archiveLogs: false
    arguments:
        parameters:
            -   description:
                    The activation code issued for this environment.
                name: activation-code
            -   description:
                    The LiferayEnvironment to activate in this namespace.
                name: liferay-environment-name
                value: {{ include "liferay.name" . }}
    entrypoint: main
    podGC:
        strategy: OnWorkflowSuccess
    securityContext:
        runAsNonRoot: true
        runAsUser: 8737
        seccompProfile:
            type: RuntimeDefault
    serviceAccountName: {{ $onlineActivationWorkflow.serviceAccountName }}
    templates:
        -   name: main
            steps:
                -   -   name: verify-pending
                        template: verify-pending
                -   -   name: write-secret
                        template: write-secret
                -   -   name: request-reconcile
                        template: request-reconcile
                -   -   name: wait
                        template: wait
        -   name: request-reconcile
            script:
                command: [sh]
                env:
                    -   name: HOME
                        value: /tmp
                    -   name: LIFERAY_WORKFLOW_NAME
                        value: "{{ "{{" }}workflow.name}}"
                image: {{ $image | quote }}
                resources:
                    {{- $onlineActivationWorkflow.resources | toYaml | nindent 20 }}
                securityContext:
                    allowPrivilegeEscalation: false
                    capabilities:
                        drop:
                            -   ALL
                    runAsNonRoot: true
                    runAsUser: 8737
                    seccompProfile:
                        type: RuntimeDefault
                source: |-
                    {{- tpl (.Files.Get "scripts/online-activation-request-reconcile.sh") . | nindent 20 }}
        -   name: verify-pending
            script:
                command: [sh]
                env:
                    -   name: HOME
                        value: /tmp
                image: {{ $image | quote }}
                resources:
                    {{- $onlineActivationWorkflow.resources | toYaml | nindent 20 }}
                securityContext:
                    allowPrivilegeEscalation: false
                    capabilities:
                        drop:
                            -   ALL
                    runAsNonRoot: true
                    runAsUser: 8737
                    seccompProfile:
                        type: RuntimeDefault
                source: |-
                    {{- tpl (.Files.Get "scripts/online-activation-verify-pending.sh") . | nindent 20 }}
        -   name: wait
            script:
                command: [sh]
                env:
                    -   name: HOME
                        value: /tmp
                image: {{ $image | quote }}
                resources:
                    {{- $onlineActivationWorkflow.resources | toYaml | nindent 20 }}
                securityContext:
                    allowPrivilegeEscalation: false
                    capabilities:
                        drop:
                            -   ALL
                    runAsNonRoot: true
                    runAsUser: 8737
                    seccompProfile:
                        type: RuntimeDefault
                source: |-
                    {{- tpl (.Files.Get "scripts/online-activation-wait.sh") . | nindent 20 }}
        -   name: write-secret
            script:
                command: [sh]
                env:
                    -   name: HOME
                        value: /tmp
                    -   name: LIFERAY_ACTIVATION_CODE
                        value: "{{ "{{" }}workflow.parameters.activation-code}}"
                image: {{ $image | quote }}
                resources:
                    {{- $onlineActivationWorkflow.resources | toYaml | nindent 20 }}
                securityContext:
                    allowPrivilegeEscalation: false
                    capabilities:
                        drop:
                            -   ALL
                    runAsNonRoot: true
                    runAsUser: 8737
                    seccompProfile:
                        type: RuntimeDefault
                source: |-
                    {{- tpl (.Files.Get "scripts/online-activation-write-secret.sh") . | nindent 20 }}
    ttlStrategy:
        secondsAfterCompletion: {{ $onlineActivationWorkflow.ttlSecondsAfterCompletion }}
{{- end -}}