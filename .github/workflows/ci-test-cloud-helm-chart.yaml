defaults:
    run:
        shell: bash
jobs:
    test-cloud-helm-chart:
        if: ${{github.repository == 'cloudnative-team/liferay-portal'}}
        permissions:
            checks: write
            contents: read
        runs-on: ubuntu-latest
        steps:
            -   name: Checkout code
                uses: actions/checkout@v4
            -   name: Install Helm
                uses: azure/setup-helm@v4
            -   name: Install helm-unittest plugin
                run: helm plugin install --verify=false --version 1.0.3 https://github.com/helm-unittest/helm-unittest
            -   name: Set up Kubeconform
                uses: bmuschko/setup-kubeconform@v1
                with:
                    kubeconform-version: 0.7.0
            -   name: Update chart dependencies
                run: helm dependency update --skip-refresh "cloud/helm/${{matrix.chart}}"
            -   name: Lint chart
                run: helm lint "cloud/helm/${{matrix.chart}}"
            -   name: Render and validate manifests
                run: |
                    set -o errexit
                    set -o nounset
                    set -o pipefail

                    function main {
                        helm template liferay "cloud/helm/${{matrix.chart}}" | \
                            kubeconform --strict --summary \
                                -schema-location default \
                                -schema-location 'https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' \
                                -skip ClusterProviderConfig,LiferayInfrastructure
                    }

                    main "${@}"
            -   name: Run unit tests
                run: |
                    set -o errexit
                    set -o nounset
                    set -o pipefail

                    function main {
                        local test_files

                        test_files=$(ls "cloud/helm/${{matrix.chart}}/tests/"*_test.yaml 2>/dev/null || echo "")

                        if [ "${test_files}" ]
                        then
                            mkdir --parents cloud/test-reports

                            helm unittest \
                                --output-file "cloud/test-reports/helm-unittest-${{matrix.chart}}.xml" \
                                --output-type JUnit \
                                "cloud/helm/${{matrix.chart}}"
                        fi
                    }

                    main "${@}"
            -   name: Upload test reports
                if: ${{!cancelled() && hashFiles(format('cloud/test-reports/helm-unittest-{0}.xml', matrix.chart)) != ''}}
                uses: actions/upload-artifact@v4
                with:
                    name: helm-test-reports-${{matrix.chart}}
                    path: cloud/test-reports/helm-unittest-${{matrix.chart}}.xml
            -   name: Publish test report
                if: ${{!cancelled() && hashFiles(format('cloud/test-reports/helm-unittest-{0}.xml', matrix.chart)) != ''}}
                uses: dorny/test-reporter@v2
                with:
                    name: Helm unit tests (${{matrix.chart}})
                    path: cloud/test-reports/helm-unittest-${{matrix.chart}}.xml
                    reporter: java-junit
        strategy:
            fail-fast: false
            matrix:
                chart:
                    -   aws
                    -   aws-infrastructure
                    -   aws-infrastructure-provider
                    -   aws-marketplace
                    -   default
                    -   gcp
                    -   gcp-infrastructure
                    -   gcp-infrastructure-provider
on:
    pull_request:
        paths:
            -   cloud/helm/**
    push:
        branches:
            -   master
        paths:
            -   cloud/**